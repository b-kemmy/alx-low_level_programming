#include <stdio.h>

/**
 * main - Prints out the letters of the alphabet a-z
 *
 * Return : Always 0 (Success)
 *
 */

int main(void)
{
	char a;

	for (a = 'a'; a <= 'z' ; a++)
	{

	putchar(a);

	}

	putchar('\n');

	return (0);

}
