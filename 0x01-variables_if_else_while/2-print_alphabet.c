#include <stdio.h>

/**
 * main - Prints out the letters of the alphabet a-z
 *
 * Returns - Always 0 (Succes)
 *
 */

int main(void)
{
	char a;

	for (a = 'a'; a <= 'z' ; a++)
	{
	
	putchar(a);
	
	}

	putchar('\n');

	return (0);

}	
