#include "main.h"

/**
 * mul - Multiplies two integers int a and int b
 *@a: a - variable
 *@b: b - variable
 * Return: 0 (Success)
 */
int mul(int a, int b)
{
	int (mul);
	mul = a * b;
	return (mul);
}
